# React Native / Expo's Metro bundler doesn't fully follow pnpm's symlinked layout,
# so transitive deps like @babel/runtime fail to resolve. A hoisted node_modules
# (npm/yarn-style) is the reliable fix for an Expo app in a pnpm monorepo.
node-linker=hoisted
